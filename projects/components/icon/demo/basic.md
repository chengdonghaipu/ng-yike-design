---
order: 0
title:
  zh-CN: 基本示例
  en-US: basic
---

## zh-CN

可以在 `<span nx-icon></span>` 标签上声明组件，也可以直接声明组件`<nx-icon></nx-icon>`，指定图标对应的 icon 属性。可以通过 type 属性来设置不同的主题风格的图标，

## en-US

There are `primary` button, `default` button, `dashed` button, `text` button and `link` button in antd.

