import { NxHiddenDirective } from './hidden.directive';

describe('HiddenDirective', () => {
  it('should create an instance', () => {
    const directive = new NxHiddenDirective();
    expect(directive).toBeTruthy();
  });
});
