import { NxSpanResponsiveDirective } from './responsive.directive';

describe('ResponsiveDirective', () => {
  it('should create an instance', () => {
    const directive = new NxSpanResponsiveDirective();
    expect(directive).toBeTruthy();
  });
});
